# Game Icons

Place 512×512 PNG (or WebP) icons here, named after the `id` field in `assets/js/games.js`. Example:

- `tile-garden-match.png`
- `ludo.png`
- `connect-4.png`

Until owner provides the actual icons, the website falls back to CSS gradients defined per game in `games.js`.
